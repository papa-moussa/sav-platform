# Ce fichier est remplacé automatiquement au démarrage par entrypoint.sh
# Voir nginx-http.conf (HTTP) et nginx-ssl.conf (HTTPS)
events { worker_connections 1024; }
http {
    server {
        listen 80;
        location / { return 503; }
    }
}
